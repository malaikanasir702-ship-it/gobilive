[build]
builder = "nixpacks"
buildCommand = "npm install && npx -p typescript tsc --project tsconfig.json"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3