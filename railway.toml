[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && npm run build:admin"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
